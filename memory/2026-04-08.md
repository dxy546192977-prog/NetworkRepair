# 工作备注 — 2026-04-08

## 背景

按《项目排查与修复方案》完成网络修复工具的结构恢复、入口修复、轻度优化与 Git 初始化。

## 已完成事项

- **P0**：将 `src/` 移回仓库根目录；`tools/` 与 `build-win-exe.sh` 归位；`其他文件/` 残余迁入 `.archive/`。
- **入口**：`Mac双击运行.app`、`Win双击运行.exe`、`set_icon.py` 与根目录 `src/` 对齐后路径恢复有效。
- **配置**：新增 `src/support/settings.json`；`network_check.sh` 增加 `load_settings()`（Python3 解析 JSON）；`network_repair.sh` 在 source 后调用，DNS 切换使用配置中的主/备 DNS。
- **分发**：根目录 `install.sh`（安装至 `~/.local/share/cursor-network-repair/<version>`，链到 `~/.local/bin/cursor-network-repair`）。
- **文档**：根 `README.md` 快速入门；`src/docs/README.md` 详细说明；原 `src/README.md` 已迁入 `docs/`。
- **仓库**：`.gitignore`（含 logs、`.archive`、`company-proxy-kit` 等）；首次提交 `8dc5e65`（v1.0.0）。

## 后续可选

- Windows 侧在路径或脚本变更后重新执行 `tools/build-win-exe.sh` 生成 EXE。
- 若需将 `company-proxy-kit` 纳入版本管理，可调整 `.gitignore` 并单独决策是否子模块化。

## Git 存档

- 本文件提交后作为当日工作闭环记录；可与 tag `v1.0.0` 或后续 tag 对照使用。
